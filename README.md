# physicsistic

Personal website. Plain HTML/CSS, no build step.

Edit `index.html` for content, `style.css` for styling, `script.js` for the starfield, orrery and scroll effects. Open `index.html` in a browser to preview.
